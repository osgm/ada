# Project npm defaults (no secrets — use `npm login` or ~/.npmrc for auth token)
engine-strict=true
save-prefix=
registry=https://registry.npmmirror.com
registry=https://repo.huaweicloud.com/repository/npm
