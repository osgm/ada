{
  "name": "@ada/contracts",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "schema:generate": "node scripts/generate-schemas.mjs",
    "schema:compat": "node scripts/check-schema-compat.mjs"
  }
}
